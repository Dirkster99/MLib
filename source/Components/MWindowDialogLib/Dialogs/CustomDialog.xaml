<Dialogs:DialogFrame x:Class="MWindowDialogLib.Dialogs.CustomDialog"
                              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                              xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                              mc:Ignorable="d" 
                              d:DesignHeight="300" d:DesignWidth="300"
                           
                              xmlns:Dialogs="clr-namespace:MWindowDialogLib.Dialogs"
                              DialogCloseResult="{Binding DialogCloseResult}"
                              DialogCanCloseViaChrome="{Binding DialogCanCloseViaChrome}"
                              xmlns:local="clr-namespace:MWindowDialogLib.Dialogs"
                              Focusable="False"
                              FocusVisualStyle="{x:Null}"
                         >
    <Grid>
        <local:DialogChrome x:Name="PART_Msg_Content"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch" />
    </Grid>
</Dialogs:DialogFrame>